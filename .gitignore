# --- secrets & deploy creds (repo is PUBLIC — keep these OUT) ---
# NB: /secrets/* (не /secrets/) — иначе негейт ниже не сработает: git не
# заходит внутрь целиком исключённой папки и !env.example игнорируется.
/secrets/*
!/secrets/env.example
*.key
*.pem
*.env
.env
.env.*
# логины/пароли Basic Auth — живут ТОЛЬКО на хосте, не в публичной репе
.htpasswd
*.htpasswd
htpasswd

# --- node (тесты фронта) ---
node_modules/

# --- build artifacts ---
/gm-screen
/bin/
/dist/
*.exe

# --- go ---
/vendor/

# --- os / editor ---
.DS_Store
*.swp
